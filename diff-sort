#!/usr/bin/env bb

; Sorts files in a .diff by the length of their changes

(ns diff-sort
  (:require [clojure.string :as str]
            diff))

(->> (slurp *in*)
     diff/files
     (sort-by count)
     (str/join "\n")
     println)
