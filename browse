#!/usr/bin/env bb

(ns browse_
  (:require [clojure.java.io :as io]
            browse))

(def static-path
  (str (.getParent (io/file *file*)) "/static"))

(when (= *file* (System/getProperty "babashka.file"))
  (browse/browse (slurp *in*) static-path))
